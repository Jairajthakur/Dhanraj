[build]
builder = "nixpacks"
buildCommand = "npm install && npm run server:build"
 
[deploy]
startCommand = "npm run server:prod"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
 
[[services]]
name = "dhanraj-backend"
 
